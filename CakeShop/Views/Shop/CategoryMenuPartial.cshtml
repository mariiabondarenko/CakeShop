@model IEnumerable<CakeShop.Models.ViewModels.Shop.CategoryVM>

<h3 align="center">Категории</h3>

<ul>
    @foreach (var item in Model) {
        
        <li><a href="/shop/category/@item.Slug.ToLower()">@item.Name</a></li>
    }
</ul>
