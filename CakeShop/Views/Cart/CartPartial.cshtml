@model CakeShop.Models.ViewModels.Cart.CartVM

    <div class="ajaxcart" style="color:white;">
        Корзина: @Model.Quantity item(s) - $@Model.Price
        <br />
        @Html.ActionLink("Главная страница", "Index")
    </div>